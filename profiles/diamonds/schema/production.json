{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Production",
  "description": "A rough diamond or parcel is extracted at a mine. The subject of the entry is the stone or parcel itself.",
  "type": "object",
  "required": ["product"],
  "properties": {
    "product": { "$ref": "defs.json#/$defs/product" }
  }
}
