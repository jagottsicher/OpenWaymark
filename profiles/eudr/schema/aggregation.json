{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Aggregation",
  "description": "Plot-level harvests pooled into a batch, or split - the regulation explicitly anticipates smallholder aggregation models. The subject of the entry is the pooled batch; the components are listed in children.",
  "type": "object",
  "required": ["action", "children"],
  "properties": {
    "action": {
      "enum": ["add", "delete"]
    },
    "container": {
      "description": "The pooled batch itself.",
      "$ref": "defs.json#/$defs/product"
    },
    "children": {
      "description": "The source plot harvests. The upper limit matches core.MaxParents.",
      "type": "array",
      "minItems": 1,
      "maxItems": 1024,
      "items": { "$ref": "defs.json#/$defs/item" }
    }
  }
}
