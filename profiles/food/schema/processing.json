{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Processing",
  "description": "Input goods become other goods: milk from three farms becomes a wheel of cheese. Corresponds to the EPCIS TransformationEvent. Unlike aggregation the inputs perish in the process - they cannot be taken apart again. The chain of origin nevertheless stays traceable, because the inputs are listed in the entry's par field.",
  "type": "object",
  "required": ["process", "inputs", "outputs"],
  "properties": {
    "process": {
      "description": "What was done, e.g. \"pasteurizing\" or \"grinding\".",
      "$ref": "defs.json#/$defs/text"
    },
    "inputs": {
      "type": "array",
      "minItems": 1,
      "maxItems": 1024,
      "items": { "$ref": "defs.json#/$defs/item" }
    },
    "outputs": {
      "type": "array",
      "minItems": 1,
      "maxItems": 1024,
      "items": { "$ref": "defs.json#/$defs/item" }
    }
  }
}
