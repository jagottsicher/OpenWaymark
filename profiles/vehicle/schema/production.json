{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Production",
  "description": "The vehicle or a major component is manufactured and receives its VIN or serial. The subject of the entry is the vehicle or component itself.",
  "type": "object",
  "required": ["product"],
  "properties": {
    "product": { "$ref": "defs.json#/$defs/product" }
  }
}
