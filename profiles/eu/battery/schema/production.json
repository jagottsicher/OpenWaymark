{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Production",
  "description": "A cell or pack is manufactured - carries the passport's core claims. The subject of the entry is the battery itself.",
  "type": "object",
  "required": ["product"],
  "properties": {
    "product": { "$ref": "defs.json#/$defs/product" }
  }
}
