{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Release",
  "description": "Third-party verification of a carbon footprint or due-diligence declaration. A claim, not a confirmation - whether the verifying party is accredited is answered by its entity trust level (OWM-6), not by this schema (spec Sec.4).",
  "type": "object",
  "required": ["certified"],
  "properties": {
    "certified": {
      "type": "boolean"
    },
    "standard": {
      "description": "The basis for the verification, e.g. \"carbon-footprint-verification\" or \"due-diligence\".",
      "$ref": "defs.json#/$defs/text"
    },
    "reference": { "$ref": "defs.json#/$defs/text" }
  }
}
