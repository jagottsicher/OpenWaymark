{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Aggregation",
  "description": "Cells assembled into a module or pack, or the reverse. The subject of the entry is the assembly; the components are listed in children.",
  "type": "object",
  "required": ["action", "children"],
  "properties": {
    "action": {
      "enum": ["add", "delete"]
    },
    "container": {
      "description": "The module or pack itself.",
      "$ref": "defs.json#/$defs/product"
    },
    "children": {
      "description": "The component cells. The upper limit matches core.MaxParents.",
      "type": "array",
      "minItems": 1,
      "maxItems": 1024,
      "items": { "$ref": "defs.json#/$defs/item" }
    }
  }
}
