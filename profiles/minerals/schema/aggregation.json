{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Aggregation",
  "description": "Batches from several sources combined before smelting, or split. The subject of the entry is the combined batch; the components are listed in children.",
  "type": "object",
  "required": ["action", "children"],
  "properties": {
    "action": {
      "enum": ["add", "delete"]
    },
    "container": {
      "description": "The combined batch itself.",
      "$ref": "defs.json#/$defs/product"
    },
    "children": {
      "description": "The source batches. The upper limit matches core.MaxParents.",
      "type": "array",
      "minItems": 1,
      "maxItems": 1024,
      "items": { "$ref": "defs.json#/$defs/item" }
    }
  }
}
