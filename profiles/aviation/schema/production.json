{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Production",
  "description": "The part is manufactured and receives its first Authorized Release Certificate (POA, FAA 8130-3 / EASA Form 1). The subject of the entry is the part itself.",
  "type": "object",
  "required": ["product"],
  "properties": {
    "product": { "$ref": "defs.json#/$defs/product" }
  }
}
